<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cuihui.rabbit.springbootproducer.dao.IOrderDao">
    <!-- 新增订单列表 -->
    <insert id="addOrder" parameterType="java.util.Map">
        insert into `t_order` (`id`, `name`, `message_id`) values(#{id},#{name},#{message_id})
    </insert>
</mapper>